name: Generate snake game

on:
  schedule:
    # Run once every 12 hours
    - cron: "0 */12 * * *"
  workflow_dispatch:
  # Run on every push to main branch to keep it updated
  push:
    branches:
      - main

jobs:
  build:
    name: Jobs to update datas
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      # Generates a snake game from a github user contributions graph
      - name: Generate github-contribution-grid-snake.svg
        uses: Platane/snk/svg-only@v3
        with:
          github_user_name: kaku-coder
          outputs: |
            dist/github-contribution-grid-snake2.svg?color_snake=blue&palette=github-dark

      # Push the content of <build_dir> to the output branch
      - name: Push github-contribution-grid-snake.svg to the output branch
        uses: crazy-max/ghaction-github-pages@v3
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
